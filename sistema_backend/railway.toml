[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --timeout 120 --workers 2"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3